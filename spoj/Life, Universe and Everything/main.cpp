#include <bits/stdc++.h>
#define ll long long
#define FIO ios_base :: sync_with_stdio(0);cin.tie(0);

using namespace std;

int main()
{
    int num=0;
	while(num!=42)
    {
        cin>>num;
        if(num!=42)
        cout<<num<<endl;
    }
}
